Development:
To get a local copy of the code, clone it using git: git clone https://github.com/rrinu/mymusic.git
Change to project directory: cd mymusic

Install dependencies:
npm install

Now, you can start a local web server by running:npm start

and then you can open http://localhost:3000 to view it in the browser.

